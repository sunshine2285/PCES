<?xml version="1.0" encoding="UTF-8"?>
<!--
  ~ Copyright (c) 2019
  ~ FileName: CollegeMapping.xml
  ~ @Author: 孙浩杰
  ~ @LastModified:2019-11-01 16:36:49
  -->

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ouc.pces.mapper.TagMapper">

    <resultMap id="resultMap" type="com.ouc.pces.entity.Tag">
        <result column="id" jdbcType="INTEGER" property="id"/>
    </resultMap>

    <resultMap id="count" type="java.lang.Integer">
    </resultMap>

    <select id="selectAll" resultMap="resultMap">
        select *
        from course
    </select>

    <select id="checkIsExist" resultMap="count">
        select count(*)
        from tag
        where courseId = #{courseId}
          and tagId = #{tagId}
    </select>

    <update id="updateTags" parameterType="List">
        update tag set num = num+1
        <foreach collection="list" item="item" index="index">
            where tagId = #{item.tagId} and courseId = #{item.courseId}
        </foreach>
    </update>

    <insert id="insertTags" parameterType="List">
        insert into tag(courseId, tagId, num)
        <foreach collection="list" item="item" index="index">
            values (#{item.courseId}, #{item.tagId}, 1)
        </foreach>
    </insert>
</mapper>